export const createSunflower = (typeId, heightId, outputId) => {
    const sunflowerObject = {
        type:typeId,
        height:heightId,
        output: outputId
    }
    return sunflowerObject
}
