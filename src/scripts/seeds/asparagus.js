export const createAsparagus = (typeId, heightId, outputId) => {
    const asparagusObject = {
        type:typeId,
        height:heightId,
        output: outputId
    }
    return asparagusObject
}
